# Show-Pressed-Keys-TR-Q-
A desktop application for graphically displahing pressed keyboard and mouse keys on the screen. It displays the pressed keys on an actual keyboard image. Currently only Turkish-Q keyboard layout is supported.
